/**
 * Retired. Home content now lives in homePage.sections.
 * Use scripts/sanity/migrate-home-builder.ts instead.
 */
console.error(
  'Este script escribía los campos planos viejos de Home. La home ahora vive en sections[]. Usa scripts/sanity/migrate-home-builder.ts.',
);
process.exit(1);
