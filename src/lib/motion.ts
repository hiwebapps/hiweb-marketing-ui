/** Shared GSAP tokens — keep motion consistent across the site. */
export const MOTION = {
  ease: 'power3.out',
  duration: 0.7,
  durationFast: 0.45,
  durationSlow: 0.9,
  stagger: 0.08,
  revealY: 24,
} as const;
